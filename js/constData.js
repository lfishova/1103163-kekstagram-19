'use strict';

(function () {
  const EFFECT_LINE = {
    MIN_LENGTH: 0,
    MAX_LENGTH: 453,
    COLOR: '#ffe753'
  };
  const PERCENT = 100;
  window.constData = {
    EFFECT_LINE: EFFECT_LINE,
    PERCENT: PERCENT
  };
})();
